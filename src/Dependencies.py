# ? src/ folder
from src.constant import *

from src import Globe # ? Import globe for manage global variable

# ? src/data folder
from data.Resource import *

# ? src/scenes folder
from src.scenes.StartScene import *